//Function that adds it's parameters to calculate a sum:
function adder(x,y,z) {
  return x+y+z;
}

//Defining parameters:
let a = "combine "
let b = "a sentence "
let c = "with the adder function!"

//Print the result
console.log(adder(a,b,c))
console.log(adder(1,2,3))
